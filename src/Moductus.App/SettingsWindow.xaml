<Window x:Class="Moductus.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Moductus"
        Width="520" Height="420"
        MinWidth="420" MinHeight="320"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        KeyDown="OnKeyDown">

    <!-- Sem estilo de propósito. Os tokens chegam no passo 5 e esta janela
         será a primeira a usá-los. -->

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="Aviso"
                   Grid.Row="0"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"
                   Visibility="Collapsed" />

        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <CheckBox x:Name="IniciarComWindows"
                      Content="Iniciar com o Windows"
                      Click="OnAutostartClick" />
            <TextBlock x:Name="AutostartDetalhe"
                       Margin="24,4,0,0"
                       TextWrapping="Wrap"
                       Opacity="0.7" />
        </StackPanel>

        <TextBlock Grid.Row="2" Text="Atalhos" FontWeight="SemiBold" Margin="0,0,0,4" />

        <ListBox x:Name="Atalhos" Grid.Row="3" />

        <TextBlock x:Name="Rodape"
                   Grid.Row="4"
                   Margin="0,12,0,0"
                   Opacity="0.7"
                   TextWrapping="Wrap" />
    </Grid>
</Window>
